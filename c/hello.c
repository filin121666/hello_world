#include <stdio.h>      // подключение возможности вывода в консоль

int main(int argc, char* argv[]) {
    printf("%s\n", "Hello, world!");    // вывод в консоль
    return 0;   // успешный выход из программы
}